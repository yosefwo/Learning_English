const body=document.body;const search=document.querySelector('#search');const theme=document.querySelector('#theme');const expand=document.querySelector('#expand');theme.addEventListener('click',()=>{body.classList.toggle('dark');localStorage.setItem('book-theme',body.classList.contains('dark')?'dark':'light')});if(localStorage.getItem('book-theme')==='dark')body.classList.add('dark');expand.addEventListener('click',()=>{const items=[...document.querySelectorAll('details')];const open=items.some(x=>!x.open);items.forEach(x=>x.open=open);expand.textContent=open?'הסתר תשובות':'הצג תשובות'});search.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();document.querySelectorAll('.chapter,.word-guide').forEach(section=>{section.classList.toggle('hidden-by-search',q&& !section.innerText.toLowerCase().includes(q))})});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',()=>{if(innerWidth<850)document.querySelector('.sidebar').scrollIntoView({behavior:'smooth'})}));